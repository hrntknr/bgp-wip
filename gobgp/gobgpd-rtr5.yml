global:
  config:
    as: 65005
    router-id: "10.0.0.50"
neighbors:
  - config:
      neighbor-address: "10.0.15.10"
      peer-as: 65001
    afi-safis:
      - config:
          afi-safi-name: ipv4-unicast
      - config:
          afi-safi-name: ipv4-flowspec
zebra:
  config:
    enabled: true
    url: "unix:/var/run/frr/zserv.api"
    redistribute-route-type-list:
      - connect